name: 02 - Building Events
on: 
    push:
    pull_request:
    schedule:
      - cron: '* * * * *'
    workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger 
        run: |
          echo "Event name: ${{ github.event_name }}"
